The text/code to search for. Use detailed, descriptive natural language queries for best results. Examples: "Which code is responsible for handling HTTP request errors with logging?", "What happens when OrderService.completeOrder() publishes an event?", "Where is a function that validates user email addresses and returns boolean?", "Where is `BaseController` referenced?". Avoid single words like "email" or "error" - the semantic search works much better with context and detail.