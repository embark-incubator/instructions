Search for code using Embark semantic search in the specified repository or repositories. This tool performs AI-powered semantic code search that understands code meaning and context, not just keyword matching. It finds relevant code even when exact terms differ. For best results, use detailed, descriptive queries that explain what you are looking for in natural language (e.g., "function that validates user email addresses and returns boolean" rather than just "email"). Returns a ranked list of file paths and code snippets with line numbers.